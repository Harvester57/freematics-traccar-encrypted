{
  "target": "c:\\Users\\Florian\\OneDrive\\Documents\\Dev\\freematics-traccar-encrypted\\server",
  "scanned_at": "2026-06-06T02:19:00+02:00",
  "focus_areas": [
    "UDP Packet Processing (server.go) — decryption, forwarding, resource management",
    "Encryption/Decryption (encryption/) — crypto correctness, nonce handling",
    "Configuration & Secrets (config.yml, entrypoint.sh, docker-compose.yml) — key handling, injection",
    "Entrypoint Shell Script (entrypoint.sh) — command/sed injection via env vars"
  ],
  "findings": [
    {
      "id": "F-001",
      "file": "src/server.go",
      "line": 103,
      "category": "sensitive-data-exposure",
      "severity": "HIGH",
      "confidence": 0.9,
      "title": "Decrypted telemetry plaintext (GPS, speed, PII) logged at INFO level",
      "description": "On line 103, the full decrypted plaintext of every client telemetry packet is logged at INFO level via logger.Infof(). The plaintext contains real-time GPS coordinates, vehicle speed, OBD-II diagnostics, and potentially PII. Line 169 logs the same plaintext a second time after successfully forwarding the encrypted response back to the client. In production, INFO-level logs are typically shipped to centralized logging (CloudWatch, ELK, etc.), making the plaintext readable by anyone with log access. This effectively undoes the encryption-in-transit protection the proxy is designed to provide.",
      "exploit_scenario": "An attacker who gains read access to the log aggregation system (e.g., via a compromised Grafana dashboard, misconfigured S3 bucket for log storage, or lateral movement within the infrastructure) can extract real-time GPS coordinates, vehicle speeds, and OBD-II data for all tracked vehicles. No decryption key is needed — the data is in plaintext in the logs.",
      "recommendation": "Log only metadata at INFO level (source IP, packet size, success/failure). Move full plaintext logging to DEBUG or TRACE level, and ensure production deployments use INFO or above. Alternatively, log a truncated hash/fingerprint of the payload for debugging purposes.",
      "confidence_reason": "Clear, verifiable data flow: decrypted plaintext is directly interpolated into log output at INFO level on two separate lines. This is not speculative."
    },
    {
      "id": "F-002",
      "file": "entrypoint.sh",
      "line": 29,
      "category": "command-injection",
      "severity": "HIGH",
      "confidence": 0.7,
      "title": "Unsanitized environment variables interpolated into sed patterns enable arbitrary file write",
      "description": "On lines 29, 34, 40, and 46 of entrypoint.sh, environment variables (CHACHA_KEY/CHACHA20_KEY, LISTEN_PORT, DEST_ADDRESS, DEST_PORT) are interpolated directly into sed substitution patterns without any sanitization or escaping. The sed delimiter is '/', so any environment variable value containing '/' will break the sed command or alter its behavior. More critically, the sed -i command operates on /tmp/config.yml, and a crafted value containing sed metacharacters could inject arbitrary replacement content into config.yml.",
      "exploit_scenario": "An attacker who controls the container's environment variables (e.g., via a compromised CI/CD pipeline, Kubernetes ConfigMap, or Docker API access) sets DEST_ADDRESS to a value like '192.168.1.1\\\"/\\nchacha_key: \"attacker_controlled_key' to inject a different encryption key into the generated config.yml, causing the server to use an attacker-known key for all encryption/decryption.",
      "recommendation": "Validate all environment variables against strict patterns before interpolation (e.g., DEST_ADDRESS must match an IP/hostname regex, LISTEN_PORT must be numeric, CHACHA_KEY must be exactly 64 hex characters). Use printf '%s' with proper quoting or a templating tool (envsubst) instead of sed with unsanitized input.",
      "confidence_reason": "The injection vector is real but requires the attacker to control container environment variables, which is a narrower attack surface than network-reachable input. Confidence lowered from max accordingly."
    },
    {
      "id": "F-003",
      "file": "src/server.go",
      "line": 119,
      "category": "resource-exhaustion",
      "severity": "HIGH",
      "confidence": 0.9,
      "title": "Unused UDP listener socket created per packet leaks file descriptors under load",
      "description": "On lines 111-124 of server.go, every successfully-decrypted packet handler goroutine creates a new UDP listener socket via net.ListenUDP(\"udp\", \":0\"). This socket (listenConn) is never used — the code subsequently creates a separate forwardConn via net.DialUDP() on line 127 and uses that for both sending and receiving. The listenConn consumes a file descriptor and an ephemeral port until the goroutine exits (defer on line 124). Under sustained traffic (e.g., a fleet of 100+ trackers reporting every second), file descriptor or ephemeral port exhaustion will crash the server or prevent new connections.",
      "exploit_scenario": "A remote attacker sends a flood of validly-encrypted UDP packets (or a compromised tracker sends at high frequency). Each packet spawns a goroutine that allocates two UDP sockets instead of one. The unused listenConn sockets accumulate until the OS limit (typically 1024 or 65535 FDs) is hit, causing subsequent net.ListenUDP or net.DialUDP calls to fail and crashing the service.",
      "recommendation": "Remove the unused listenConn socket creation entirely (lines 111-124). The forwardConn on line 127 already handles both sending to and receiving from the backend. This also eliminates one of the two socket allocations per packet.",
      "confidence_reason": "The unused socket is clearly visible in the code. listenConn is never referenced after creation except for the deferred Close(). Already flagged as T5 in the project's own THREAT_MODEL.md."
    },
    {
      "id": "F-004",
      "file": "src/server.go",
      "line": 99,
      "category": "sensitive-data-exposure",
      "severity": "MEDIUM",
      "confidence": 0.6,
      "title": "Full hex dump of failed-decryption packets logged, enabling log flooding and information leakage",
      "description": "On line 99 of server.go, when decryption fails, the entire raw packet is hex-encoded and logged at WARN level: rawHex := hex.EncodeToString(buf[:n]). For a maximum UDP packet of 1500 bytes, this produces 3000 characters of hex per log line. An attacker can flood the listener port with garbage packets, each generating a WARN-level log entry with the full 3KB hex dump. This can fill disk, overwhelm log aggregation, or cause log-based alerting fatigue. Additionally, if a legitimate packet fails decryption due to a transient error, the raw ciphertext is exposed in logs.",
      "exploit_scenario": "A remote attacker sends thousands of random 1500-byte UDP packets per second to the listener port. Each packet fails decryption and generates a ~3KB WARN log line. At 10,000 packets/second, this produces ~30MB/s of log output, filling disk and overwhelming the logging infrastructure.",
      "recommendation": "Log only a truncated prefix of the raw hex (e.g., first 32 bytes) and the total length. Rate-limit decryption failure logging (e.g., log one summary every N failures or per source IP per interval).",
      "confidence_reason": "The logging of full raw payloads is confirmed in the code. The log-flooding attack is straightforward but is more of a defense-in-depth concern than a direct compromise."
    }
  ],
  "summary": {"total": 4, "high": 3, "medium": 1, "low": 0, "low_confidence": 0}
}
